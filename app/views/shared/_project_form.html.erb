<%= form_for(@project) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
      <%= f.text_area :projectTitle, placeholder: "Project Title...", size: "30x50" %>
    </div>
    <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>